<p>
  <strong>Available Categories:</strong>
  <%= @category.name %>
  <div>
    <%= link_to 'Edit Destroy', edit_category_path(@category), class:"btn btn-primary me-2" %>
    <%= link_to "Destroy Destroy", category_path(@category), data: { turbo_method: :delete }, class:"btn btn-danger" %>
  </div>
</p> 